#include <iostream>
#include <cmath>
using namespace std;

int main(){
	int n;
	cin >> n;
	for(int i=1; i<=n; i++){
		int potencia2 = pow(2,i);
			if(potencia2 <= n)
				cout << potencia2;
	
	}
  return 0;
  }
